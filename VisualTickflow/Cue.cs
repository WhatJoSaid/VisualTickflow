namespace VisualTickflow
{
    public class Cue
    {
        public Cue(string game, string cue, string[] parameters, float beat)
        {
            switch (game)
            {
                case "spaceball":
                    
                    break;
            }
        }
    }
}